$(function() {
    $(window).resize(function() {
        a();
        console.log(1)
    });
    a();
    function a() {
        var b = $(".activePage").height() / 2 + 30,
        c = $("body").width() - 80;
        $(".activePage").css({
            "width": c,
            "marginLeft": -c / 2
        })
    }
});
function doPay(c) {
    var a = U("Charge/doActivityCharge");
    var b = {
        activity_id: c
    };
    ajaxPost(a, b,
    function(d) {
        window.location.href = d.url
    })
};