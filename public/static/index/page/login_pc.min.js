$(function() {
    b();
    $(window).resize(function() {
        b()
    });
    function b() {
        var c = $("body").width();
        $(".cartoonWapper").width(c - 20);
        $(".footer").width(c - 60);
        $(".bootomDetele").width(c);
        var e = $(window).height(),
        d = $(".cartoonWapper").innerHeight();
        $(".wxLoginBg").height(e - d)
    }
    if (code) {
        setInterval(a, 2000)
    }
    function a() {
        var c = U("checkQrLogin");
        $.ajax({
            type: "post",
            url: c,
            data: {
                code: code
            },
            dataType: "json",
            success: function(d) {
                if (d.msg === "ok" && d.data.url) {
                    window.location.href = d.data.url
                }
            }
        })
    }
});